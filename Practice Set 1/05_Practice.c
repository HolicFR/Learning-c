// Custom Values To measure

#include <stdio.h>

int main() {

    int r;
    int h;

        printf("Input Radius of the circle: ");
        scanf("%d", &r);

        printf("Input Height: ");
        scanf("%d", &h);

            printf("The volume of cylinder is: %.1f\n", 3.14*r*r*h);
    return 0;
}