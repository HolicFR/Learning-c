#include <stdio.h>

int main() {
       int a, b, c;
       
       a = 10; b = 5; c = 7;

       if (a>=b && a>= c){
        printf("a is greater than b and c \n");
      
    }
    printf("a: %d b: %d c:%d", a, b, c);

    return 0;
}