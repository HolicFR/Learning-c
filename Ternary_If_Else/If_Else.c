#include <stdio.h>

int main() {
    int num = -5; 

     // Check if num is greater than or equal to 0
     if (num >= 0) {
        printf("Positive");
     } else {
        printf("Negative");
     }
     
    return 0;
}