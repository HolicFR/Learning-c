// 6. Check if number is positive or negative
#include <stdio.h>

int main() {
    int num = -10;

    // If num >= 0 → Positive
    // Otherwise → Negative
    (num >= 0) ? printf("Positive") : printf("Negative");

    return 0;
}