// Check if someone is adult

#include <stdio.h> 
    int main() {
            int age = 20;

            // 18 or above = adult
            if (age >= 18) {
                printf("adult");
            } else {
                printf("Minor");
            }
            return 0;
    }