#include <stdio.h>
 int main () {

    char myGrade = 'A' ;

    printf("He's got an \"%c\" for his performance.", myGrade); //To print with a format specifier use ("") between the sentence in printf before seperating the line with the variable like (myGrade)
    

    //If you want the format specifier output to be in ("") commas use \"(FORMAT SPECIFIER HERE)\" this format.
    
        return 0;

 }
 