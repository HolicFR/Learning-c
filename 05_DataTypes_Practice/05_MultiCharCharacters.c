#include <stdio.h>
 int main() {

    char myText[] = "Hello";
    printf("%s"), myText);

    return 0;
}

// To store multiple characters (or whole words), use strings
// Will learn more about *STRINGS* later.
