#include <stdio.h>

int main() {
    int x = 10;
    int y = 20; // USING > (Greater than)

    printf("%d", x > y); // Returns 1 if the first value is greater than the second value
                        

                    // Here it returns 0 because the value of X is less than Y 
    return 0;
}