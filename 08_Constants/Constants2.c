#include <stdio.h>

int main() {
const int minutesperhour = 60;
const int monthsinyear = 12;

    printf("There are %d minutes per hour \n", minutesperhour);
    printf("There are %d Months in a Year", monthsinyear);
    return 0;
}

// This is the right usage of *Constants* in C